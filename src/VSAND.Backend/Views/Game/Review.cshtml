@model VSAND.Data.ViewModels.GameReport.GameReport;

@{
    ViewData["Title"] = Model.Name + "| Review";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

@{ await Html.RenderPartialAsync("_headerPartial"); }

<div id="vueApp" v-cloak>
    <widget-wrapper icon="eye" title="Review"
                    v-bind:padding="true">
        <template v-slot:toolbarmasterbuttons>
            @{ await Html.RenderPartialAsync("_gameNav.cshtml"); }
        </template>

        Review

        <template v-slot:footer>
            Save Button Goes Here
        </template>
    </widget-wrapper>
</div>


@section Scripts {
    <script type="text/javascript">
        window.gameReport = @Html.Raw(Newtonsoft.Json.JsonConvert.SerializeObject(Model));
    </script>

    <environment include="Development">
        <script src="~/app/game-review.js" asp-append-version="true"></script>
    </environment>
    <environment exclude="Development">
        <script src="~/app/game-review.min.js" asp-append-version="true"></script>
    </environment>
}
