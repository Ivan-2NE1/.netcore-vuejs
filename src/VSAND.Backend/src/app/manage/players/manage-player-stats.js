
import WidgetWrapper from "../../components/base/widget-wrapper/widget-wrapper.vue";

var playerStats = new Vue({
    el: '#vueApp',

    components: {
        "widget-wrapper": WidgetWrapper
    }
});
